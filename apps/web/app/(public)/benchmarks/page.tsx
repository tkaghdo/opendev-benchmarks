import { StubPanel } from "@/components/StubPanel";

export default function BenchmarksPage() {
  return (
    <section>
      <h1>Benchmarks</h1>
      <p className="lede">
        Rank organizations on cycle time, first review, throughput, issue resolution, contributor
        community, growth, and concentration.
      </p>
      <StubPanel build="Build 8">
        Rankings, percentiles, homepage highlights, and the 15–25 org catalog are after launch. Launch
        uses five organizations and org/compare pages.
      </StubPanel>
    </section>
  );
}
