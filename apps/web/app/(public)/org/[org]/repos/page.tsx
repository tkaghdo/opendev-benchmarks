import { StubPanel } from "@/components/StubPanel";

export default function ReposPage() {
  return (
    <StubPanel build="Build 4">
      Repository table: stars, language, PR throughput, cycle time, review time, issue backlog, and
      commit activity. Per-repo pages land in Build 10.
    </StubPanel>
  );
}
