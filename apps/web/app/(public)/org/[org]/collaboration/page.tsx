import { StubPanel } from "@/components/StubPanel";

export default function CollaborationPage() {
  return (
    <StubPanel build="Build 10">
      Collaboration covers time to first review, reviews per PR, reviewer workload, and
      author/reviewer patterns.
    </StubPanel>
  );
}
