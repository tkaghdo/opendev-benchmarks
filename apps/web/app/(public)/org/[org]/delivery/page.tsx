import { StubPanel } from "@/components/StubPanel";

export default function DeliveryPage() {
  return (
    <StubPanel build="Build 4 / 10">
      Delivery answers how quickly this organization ships software: PR throughput, cycle time,
      commits per week, and repository activity, with org → repo drill-down.
    </StubPanel>
  );
}
