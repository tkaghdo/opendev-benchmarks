import { StubPanel } from "@/components/StubPanel";

export default function ContributorsPage() {
  return (
    <StubPanel build="Build 4">
      Active, new, and returning contributors, plus top-10 concentration — what share of activity
      comes from a small set of developers.
    </StubPanel>
  );
}
